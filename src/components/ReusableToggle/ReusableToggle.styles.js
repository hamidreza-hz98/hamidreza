import { styled, Grid, RadioGroup } from "@mui/material";

export const ToggleGroup = styled(RadioGroup)({
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  marginTop: "12px",
});
